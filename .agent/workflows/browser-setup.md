---
description: WSL2のブラウザ操作環境（socatブリッジとChrome起動）をセットアップします
---

1. ブラウザブリッジスクリプトを実行します // turbo
2. `scripts/browser-bridge.sh` を実行する

3. 接続を確認します

```bash
curl -s http://127.0.0.1:9222/json/version
```
